{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_TOOL_INPUT\" | python3 -c \"import sys,json; d=json.load(sys.stdin); exit(0 if \\\"git push\\\" in d.get(\\\"command\\\",\\\"\\\") else 1)\" 2>/dev/null; then cd homework-6 && python -m pytest tests/ --cov=agents --cov-fail-under=80 -q 2>&1; fi'"
          }
        ]
      }
    ]
  }
}
